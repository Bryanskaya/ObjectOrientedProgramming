#ifndef BASEITERATOR_H
#define BASEITERATOR_H


class BaseIterator
{
protected:
    int index = 0;
    int num_elem = 0;

public:
    BaseIterator();
    ~BaseIterator();
    // фигня с задаванием числа
};

#endif // BASEITERATOR_H
